<div class="jumbotron">
    <p>Liste des meetups :</p>

    <ul>
        <?php foreach ($meetups as $meetup): ?>
            <li>
                <a href="<?php echo $meetup['id'] ?>"><?php echo $meetup['title'] ?></a>
                <?php echo $meetup['startingDate']->format('d-m-Y') ?>
            </li>
        <?php endforeach; ?>
    </ul>
</div>